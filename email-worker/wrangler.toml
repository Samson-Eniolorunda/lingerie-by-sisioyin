name = "lbs-email-reply"
main = "src/index.js"
compatibility_date = "2024-12-01"

# The webhook URL is set as a secret, not a var, for security.
# Run: npx wrangler secret put WEBHOOK_URL
# Value: https://oriojylsilcsvcsefuux.supabase.co/functions/v1/receive-email-reply

# Email routing is configured in the Cloudflare dashboard:
#   Email Routing → Routes → Catch-all → Send to Worker → lbs-email-reply
